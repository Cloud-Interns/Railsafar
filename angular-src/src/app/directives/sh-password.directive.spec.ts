import { ShPasswordDirective } from './sh-password.directive';

describe('ShPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ShPasswordDirective(this.el);
    expect(directive).toBeTruthy();
  });
});
